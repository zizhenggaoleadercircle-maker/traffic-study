# .config

Use this folder for repository configuration that should be versioned: editor settings snippets, CI-related config references, or shared tool configs.

Do not put secrets here; use `.env` at the repo root (gitignored) for local credentials.
